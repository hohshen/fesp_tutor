apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: {{ .Values.namespace  | quote }}
spec:
  type: NodePort
  selector:
    app: app-deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000